// SpecialOffer.tsx
import React from 'react';


interface SpecialOfferProps {
  title: string;
  description: string;
}

const SpecialOffer: React.FC<SpecialOfferProps> = ({ title, description }) => {
  return (
    <div className="special-offer-card">
      
    </div>
  );
};

export default SpecialOffer;
